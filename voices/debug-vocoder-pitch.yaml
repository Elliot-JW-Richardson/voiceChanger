id: debug-vocoder-pitch
name: "DEBUG: Vocoder + Pitch Shift"
default: false
# DIAGNOSTIC VOICE -- see debug-vocoder-only.yaml's header comment for
# context. DELETE once the diagnosis is complete.
#
# Isolates STAGE 2 of Magos's chain: vocoder followed by
# pitch_shift(-10 semitones), matching magos.yaml's current ordering
# exactly, but WITHOUT the final bitcrush step. Compare against
# debug-vocoder-only (stage 1) to isolate what pitch_shift's phase-vocoder
# processing does to the vocoder's already-synthetic, broadband output.
chain:
  - type: vocoder
  - type: pitch_shift
    semitones: -10
